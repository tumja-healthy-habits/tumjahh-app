import { styles } from "../styles";
import { Text, View } from "react-native";

export default function FeedScreen() {
    return (
        <View style={styles.container}>
            <Text style={styles.textfieldTitle}>Your Feed</Text>
        </View>
    )
}
